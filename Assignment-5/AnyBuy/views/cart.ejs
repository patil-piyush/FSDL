<!DOCTYPE html>
<html>
<head>
    <title>Your Cart - AnyBuy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/home">AnyBuy</a>
            <a href="/home" class="btn btn-outline-light">Back to Shopping</a>
        </div>
    </nav>

    <div class="container">
        <h2>Your Cart</h2>
        <% if(cart.items.length === 0) { %>
            <div class="alert alert-info mt-3">Your cart is empty.</div>
        <% } else { %>
            <table class="table mt-4">
                <thead>
                    <tr>
                        <th>Product</th>
                        <th>Price</th>
                        <th>Quantity</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    <% let total = 0; %>
                    <% cart.items.forEach(item => { %>
                        <% let subtotal = item.productId.price * item.quantity; %>
                        <% total += subtotal; %>
                        <tr>
                            <td>
                                <strong><%= item.productId.name %></strong>
                            </td>
                            <td>$<%= item.productId.price %></td>
                            <td><%= item.quantity %></td>
                            <td>$<%= subtotal %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
            <div class="text-end">
                <h3>Total: $<%= total %></h3>
                <button class="btn btn-success btn-lg mt-2">Checkout</button>
            </div>
        <% } %>
    </div>
</body>
</html>